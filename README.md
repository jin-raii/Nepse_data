# 🚀 UV Package Manager Guide (Windows, macOS, Linux)

## 📌 What is `uv`?
[`uv`](https://github.com/astral.sh/uv) is a fast Python package manager, an alternative to `pip`, `venv`, and `pip-tools`.

---

## 🔹 Installation Guide

### **Windows**
```powershell
winget install astral.sh.uv
```
### **Mac**
```
brew install uv

```

